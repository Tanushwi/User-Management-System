<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">UMS</a>
    <div class="d-flex align-items-center">
      <div id="notifBadge" class="me-3 text-white">Notifications: <span id="notifCount">0</span></div>
      <% if (user) { %>
        <div class="text-white me-3">Hi, <strong><%= user.name %></strong></div>
        <form method="post" action="/auth/logout"><button class="btn btn-outline-light">Logout</button></form>
      <% } else { %>
        <a class="btn btn-outline-light me-2" href="/auth/login">Login</a>
        <a class="btn btn-light" href="/auth/register">Register</a>
      <% } %>
    </div>
  </div>
</nav>
